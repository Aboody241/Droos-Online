import 'package:flutter/material.dart';

// ألوان المشروع الرئيسية
class AppColors {
  static const primary = Color.fromARGB(255, 0, 94, 255);       // أزرق رئيسي
  static const secondary = Color(0xFF50E3C2);     // أخضر فاتح
  static const background = Color(0xFFF5F5F5);    // خلفية فاتحة
  static const card = Colors.white;               // لون الكروت
  static const textPrimary = Colors.black87;     // نص أساسي
  static const textSecondary = Colors.black54;   // نص فرعي
  static const error = Colors.redAccent;         // رسائل خطأ
  static const success = Colors.green;           // حالات نجاح
}
