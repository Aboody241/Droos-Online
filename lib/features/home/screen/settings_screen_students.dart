import 'package:flutter/material.dart';

class SettingsStudentScreen extends StatelessWidget {
  const SettingsStudentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Settings Screen"),
    );
  }
}